{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "copy-config-files",
            "type": "shell",
            "command": "cp -f conf/.devops.yaml bin/ && mkdir -p config && cp -f conf/RobotNotice.yaml config/RobotNotice.yaml",
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "problemMatcher": [],
            "presentation": {
                "reveal": "silent",
                "panel": "shared",
                "showReuseMessage": false
            }
        }
    ]
}
